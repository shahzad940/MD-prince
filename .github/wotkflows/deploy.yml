name: Node js Cl on: push: branches: -main pull request: branches: -main jobs: build: runs on: ubuntu-latest strategy: mname: Node js Cl on: push: branches: -main pull request: branches: -main jobs: build: runs on: ubuntu-latest strategy: matrix: nodeversion: (20.x] steps: - name: Checkout repository uses: actions/checkout@v3 - name: Set up Node,js uses: actions/setup-node@N3 with: nodeversion: S{{ matrix.node-version )} - name: Install dependencies run: npm install - name: Start application un: npm start
